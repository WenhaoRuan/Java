Unlike C++, which is what PRO1 course is focused on, vectors in java aren't the main way of handling an array of same 
kind elements. This is due the fact that in java the container works in a very different way, it's use represents
a giant synchronitzation overhead, locking overhead and is broadly considered legacy and obsolete.
Instead in java the main method of containing elements of a type is with arraylist, which is what I'm gonna use to solve these problems.
